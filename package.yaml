name: hcrl
ghc-options: -O2 -Wall
dependencies:
  - base >= 4.14 && < 5
  - relude

  - websockets
  - wreq
  - wuss

  - file-embed
  - JuicyPixels

  - aeson
  - aeson-pretty
  - astar
  - base64
  - containers
  - data-default
  - delay
  - fixed-vector
  - lens
  - megaparsec
  - random
library:
  source-dirs: .
  exposed-modules:
    - Game
    - Slack
executable:
  main: Main.hs
  source-dirs: .
