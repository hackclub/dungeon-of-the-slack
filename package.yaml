name: hcrl
dependencies:
  - base >= 4.14 && < 5
  - relude

  - websockets
  - wreq
  - wuss

  - aeson
  - delay
  - fixed-vector
  - lens
  - megaparsec
library:
  source-dirs: .
  exposed-modules:
    - Game
    - Slack
executable:
  main: Main.hs
  source-dirs: .
